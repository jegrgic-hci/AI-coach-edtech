# Dashboards & data surfaces

Stub — not yet populated with project-specific findings. Apply the general lenses in `SKILL.md` directly until this file is built out from real review sessions.

Note the distinction already established in this project's design guidance: a dashboard or tool is scanned and operated, not read top-to-bottom — craft shifts from typography toward information design (state encoded in form as well as number: a pill, a chip, a severity stripe). See the `dataviz` skill for chart/visualization-specific guidance, and `teacher-dashboard-design.md` for this project's existing IA and flag/signal system on `app/web/dashboard.html` specifically — read that before proposing anything new for that surface, it already encodes prior decisions.

Also see the "no decorative sparklines" standing preference in project memory before adding any trend visual to a dashboard surface.
