# Environment & Config (contains secrets — never commit)
.env
.env.local
.env.*.local
config.json

# Path B dev store (student data shape — never commit)
app/data/

# Node / build
node_modules/
dist/
build/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
AI coach api key.rtf

# Old files
old/

# Lab pages — local prototyping benches, never committed and never deployed.
# They are already excluded from the Docker image by .dockerignore's matching
# rule; this keeps them out of the repo as well. Three of them had been
# committed before 2026-08-18, which is what prompted the rule.
app/web/*-lab.html
